import React from 'react';
import CartBlock from '../components/cartBlock/CartBlock';

import '../scss/app.scss';

const Cart: React.FC = () => {
  return (
    <div>
      <CartBlock />
    </div>
  );
};

export default Cart;
